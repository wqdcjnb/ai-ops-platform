# 首页界面待办记录

> 记录日期：2026-09-18  
> 状态：仅记录，暂不实施

## 背景

当前首页（`/`）仍直接展示运营平台 BFF、New API、CPA 实验服务等服务信息。后续系统会引入独立网关，因此首页上的服务探测、配置检查和系统入口需要统一调整为独立网关视角。

## 待调整区域

### 1. 服务矩阵

- 当前标题：`服务矩阵`
- 当前内容：展示运营平台 BFF、New API、CPA 实验服务的连通/可用状态和最近探测时间。
- 后续方向：改为展示独立网关及其下游服务的真实探测结果。
- 需要确认：
  - 独立网关的正式名称和展示层级；
  - 网关本身与下游服务是否分层展示；
  - 探测接口、状态枚举和刷新频率。

### 2. 首次配置清单

- 当前标题：`首次配置清单`
- 当前进度：`3/4`。
- 当前步骤：New API 服务、New API 管理认证、CPA 隔离实验服务、客户端链路验证。
- 后续方向：将配置项改为独立网关接入、网关鉴权、下游服务连通性和客户端链路验证等步骤。
- 需要确认：
  - 独立网关是否作为首个必需配置项；
  - 网关鉴权与下游服务认证的职责边界；
  - 客户端链路验证是否仍保留为单独步骤。

### 3. 系统入口

- 当前标题：`系统入口`
- 当前内容：提供 New API 管理后台、CPA 管理后台的地址入口。
- 后续方向：入口统一切换为独立网关提供的安全地址，不在页面中直接暴露各后端管理服务地址。
- 需要确认：
  - 独立网关入口地址及环境差异（开发、测试、生产）；
  - 是否只保留一个网关入口，或按业务保留多个安全入口；
  - 地址是否由后端动态返回。

## 实施约束

- 本次只建立记录，未修改任何页面、接口或配置。
- 后续实施时，服务矩阵、配置清单、系统入口应使用同一套独立网关配置来源，避免三处文案和状态不一致。
- 不在前端写死生产地址、凭据或 Token；环境地址由本地配置或后端安全接口提供。

## 后续补充

待确认独立网关的产品命名、接口契约、状态模型和环境地址后，再拆分为具体开发任务。

## 运营总览页待办

页面路径：`/overview`  
状态：仅记录，暂不实施

### 4. 本月成本点数

- 当前内容：指标卡展示“本月成本点数”、当前数值、环比百分比和“目标 12,000 点”。
- 反馈：独立网关接入后，该指标的来源和意义尚未确定，可能会被放弃。
- 后续方向：待独立网关的计量口径明确后，再决定保留、改名、改为网关计量，或移除该指标。
- 需要确认：
  - 成本点数是否仍由平台内部计算；
  - 独立网关是否提供统一用量/成本数据；
  - 是否需要对接供应商账单，而不是继续展示内部点数。

### 5. 用量趋势中的成本点数

- 当前内容：`用量趋势` 同时绘制请求数和内部成本点数，并注明“不等同于供应商实际账单”。
- 反馈：成本点数与独立网关接入后的计量方式同样需要重新评估。
- 后续方向：与“本月成本点数”使用同一计量口径；如果无法形成稳定且可解释的数据，应移除成本点数序列，只保留请求/用量趋势。
- 需要确认：
  - 趋势图是否改为网关请求量、Token 用量或实际账单金额；
  - 7 天/30 天切换是否继续保留；
  - 图例和说明是否需要明确数据来源。

### 6. 需要关注

- 当前内容：展示官方全球组错误率升高、CPA Lab 5xx、官方全球账号余额偏低等事件。
- 反馈：独立网关接入后，告警对象、错误边界和事件来源都需要调整。
- 后续方向：改为围绕独立网关、网关下游服务和实际可执行动作生成事件，避免继续直接绑定旧服务名称。
- 需要确认：
  - 事件是否由独立网关统一汇聚；
  - 网关错误与下游服务错误如何区分；
  - 事件跳转目标、优先级和处理状态是否需要重做。

### 7. 人员消耗排行中的“个人目标”和“成本点数”

- 当前区域：人员消耗排行表头的“个人目标”和“成本点数”字段。
- 反馈：这两个字段当前意义不明确，用户难以判断其计算口径和用途。
- 后续方向：在业务定义明确前，不应继续保留含义模糊的字段；可改为明确的用量、请求数、Token 数或实际成本字段，也可以暂时隐藏。
- 需要确认：
  - “个人目标”是否有真实的配置和考核来源；
  - “成本点数”是否与独立网关统一计量；
  - 是否需要保留人员维度，还是改为团队/业务维度。

## 运营总览页实施约束

- 上述 4 项与独立网关计量和告警模型强相关，暂不单独改文案或前端字段。
- “本月成本点数”和趋势图成本点数必须共用同一数据定义，不能一处替换、另一处继续使用旧口径。
- “需要关注”应优先接入独立网关事件模型后再调整，避免先做一次临时映射。
- “个人目标”和“成本点数”在定义确认前，默认列为待删除/待重命名字段，不作为既定需求。

## 人员与部门页待办

页面路径：`/people`  
状态：仅记录，暂不实施

### 8. 批量导入 CSV 内容本地化

- 当前入口：`批量导入`。
- 反馈：导入弹窗中的 CSV 文件内容需要全部使用中文。
- 后续方向：CSV 模板的表头、示例数据、字段说明、填写提示和校验错误统一中文化。
- 需要确认：
  - 中文字段名对应的后端字段映射；
  - 是否继续兼容现有英文表头；
  - 下载模板和导入失败反馈是否使用同一套字段命名。

### 9. 离职待回收文案

- 当前内容：统计卡显示“离职待回收 1”，辅助说明为“需要检查并停用 Key”。
- 反馈：卡片标题改成“待回收”。
- 后续方向：统一使用“待回收”作为状态名称，并在说明或详情中表达离职人员、Key 回收等具体原因，避免把原因和状态混在标题里。
- 需要确认：
  - “待回收”是否包含离职、转岗、停用等多种原因；
  - 详情列表是否需要显示具体回收原因和处理动作。

### 10. 部门概览动态化

- 当前内容：页面以卡片形式展示若干部门及人数、Key 数量和占比。
- 反馈：真实情况可能不止当前展示的几个部门。
- 后续方向：部门概览应从真实部门数据动态生成，不使用固定的少量部门列表；当部门数量增加时，需要明确换行、滚动、分页或“查看更多”的呈现方式。
- 需要确认：
  - 部门数据来源和刷新方式；
  - 是否展示全部部门，还是按人数/活跃度排序后展示 Top N；
  - 部门为空、部门名称过长和部门数量较多时的展示规则。

## 人员与部门页实施约束

- 批量导入的中文字段必须与后端解析规则保持一致，不能只替换界面文案。
- “待回收”属于状态命名调整，实施时需要同步卡片、筛选项、列表状态和详情页文案。
- 部门概览不得继续依赖前端硬编码部门数组，应以接口返回的部门集合为准。

## Key 管理页待办

页面路径：`/keys`  
状态：仅记录，暂不实施

### 11. 客户端连接规范

- 当前内容：页面底部展示“客户端连接规范”，说明员工使用平台地址和个人 Key，并展示 `http://127.0.0.1:3000/v1` 连接地址及复制操作。
- 反馈：独立网关接入后，这块内容后期可能没有用途。
- 后续方向：待独立网关的客户端访问方式确定后，再决定移除该区域，或改为展示独立网关提供的统一接入地址和调用规范。
- 需要确认：
  - 客户端是否仍通过个人 Key 访问；
  - 独立网关是否替代当前平台地址和 `/v1` 路径；
  - 员工端是否还需要在 Key 管理页查看或复制接入地址。

## Key 管理页实施约束

- 暂不直接删除“客户端连接规范”，避免在独立网关地址和鉴权方式未确定前丢失现有接入说明。
- 后续若保留，应统一使用独立网关返回的环境地址，不在前端写死 `127.0.0.1` 或旧服务地址。

## 额度与限流页待办

页面路径：`/limits`  
状态：待确认，暂不删除

### 12. 额度与限流模块的业务必要性

- 当前入口：侧边栏中的“额度与限流”。
- 当前内容：公司月度目标、已用与预留额度、接近目标范围、本月限流次数、软额度模式，以及按公司/部门/人员/用途/Key 展开的额度树。
- 反馈：当前模块整体含义不明确；如果与核心业务关联不大，可以删除。
- 后续方向：先确认该模块是否服务于真实的业务决策、预算控制或网关限流策略，再决定保留、重做或下线。
- 需要确认：
  - 额度数据是否来自独立网关或供应商真实配额；
  - “额度目标”“成本点数”“限流”三者的定义和关系；
  - 是否存在必须由管理员配置的限额、预警和阻断规则；
  - 页面中的层级树是否是实际业务组织结构，还是当前演示数据。

## 额度与限流页实施约束

- 在业务归属和数据来源未确认前，不直接删除路由、导航项或相关接口，避免误删核心管控能力。
- 若确认与核心业务无关，应整体下线入口、页面、状态卡片和对应的模拟数据，而不是只隐藏侧边栏菜单。
- 若独立网关承担限流职责，应重新评估该页面是否改为网关策略配置页，并统一使用网关返回的配额与策略状态。

## 用途与路由页待办

页面路径：`/routes`  
状态：待确认，暂不删除

### 13. 用途与路由模块的业务必要性

- 当前入口：侧边栏中的“用途与路由”。
- 当前内容：业务用途、正式路由、实验路由、需要关注数量，以及正式与实验路由隔离说明；列表中按用途、别名、渠道和环境查看模型路由。
- 反馈：当前模块整体意义不明确。
- 后续方向：先确认它是否承担核心的模型选择、渠道分流、实验隔离或权限控制职责，再决定保留、重做，或与独立网关能力合并。
- 需要确认：
  - 路由规则实际由谁执行：平台、独立网关，还是上游服务；
  - “业务用途”“正式路由”“实验路由”的定义和使用者；
  - 实验路由是否仍需要在生产环境单独管理；
  - 当前列表数据是否为真实配置，还是演示数据。

## 用途与路由页实施约束

- 在路由执行边界和核心业务归属未确认前，不直接删除路由、导航项或相关数据结构。
- 如果独立网关已经统一承担路由、分流和实验隔离，应评估将本页改为网关策略管理，而不是继续维护重复的前端概念。
- 若确认与核心业务无关，应整体下线页面入口、路由配置、列表和模拟数据，并同步清理相关权限与接口。

## 模型与渠道页待办

页面路径：`/models`  
状态：待替换，暂不实施

### 14. 移除模拟数据入口

- 当前内容：页面提供“模拟数据 / New API 数据”数据源切换，并以模拟数据展示模型目录、可用模型、渠道健康度和降级项。
- 反馈：模拟数据入口可以删除，后续替换为真实数据。
- 后续方向：移除“模拟数据”切换、模拟数据标识和依赖模拟数据的展示逻辑；数据统一来自真实服务或独立网关接口。
- 需要确认：
  - 真实数据接口和字段契约；
  - 无真实数据、接口异常和加载中的空状态；
  - 是否保留本地开发环境的 mock 能力（如保留，应放在开发配置或测试工具中，不出现在业务页面）。

### 15. 统一正式环境，取消实验环境

- 当前内容：模型目录中存在“CPA 高能力实验”等模型名称，并使用“实验”环境标签。
- 反馈：后续所有环境都是正式环境，不再有实验环境。
- 后续方向：移除实验环境筛选、标签和文案；将相关模型改为正式环境命名，并统一使用正式环境状态。
- 需要确认：
  - “CPA 高能力实验”正式名称；
  - 后端环境枚举是否同步删除 `experiment`；
  - 渠道健康矩阵、路由配置和告警中是否仍存在实验环境分支。

## 模型与渠道页实施约束

- 真实数据接入完成后再删除模拟数据依赖，避免页面进入无数据状态时缺少可诊断信息。
- 环境统一为正式环境时，需要同步清理模型目录、筛选器、标签、统计卡片、健康矩阵和相关接口中的实验分支。
- 不在前端保留“实验”作为默认或隐藏选项，避免与实际环境模型产生歧义。

## 上游账号页待办

页面路径：`/upstreams`  
状态：待真实网关接入后调整，暂不实施

### 16. 实时服务连通性

- 当前内容：页面顶部展示 New API 和 CPA 实验服务的实时探测状态，并以 LIVE 标识服务可达性。
- 反馈：后期接入真实网关后，这两个服务连通性展示可以不用。
- 后续方向：真实网关接入并成为统一探测入口后，移除针对旧服务的独立实时连通性卡片；如仍需要健康状态，应改为展示网关及其下游的统一状态。
- 需要确认：
  - 独立网关是否提供统一健康检查接口；
  - 是否需要展示下游服务明细，还是只保留网关总体状态；
  - 旧的 New API/CPA 探测接口和状态字段是否同步废弃。

### 17. 取消实验账号和实验环境

- 当前内容：账号列表包含“CPA Pro · 实验账号”卡片、`CPA PRO · 实验` 标签和实验环境状态。
- 反馈：后续不要实验环境。
- 后续方向：移除所有实验账号、实验标签、实验筛选项和实验环境枚举；保留的账号统一归入正式环境，并使用正式命名。
- 需要确认：
  - 实验账号对应的真实账号是否需要迁移为正式账号；
  - 后端账号类型、环境字段和权限是否同步删除实验分支；
  - 账号统计、健康矩阵、告警和详情页是否仍引用实验账号。

## 上游账号页实施约束

- 真实网关接入完成前，不直接删除现有探测展示，避免暂时失去连通性诊断能力。
- 取消实验环境时，需要同时清理账号卡片、筛选器、统计数字、状态标签、接口数据和权限逻辑。
- 页面不应继续显示“正式与实验账号已隔离”等与实际环境模型不一致的提示。

## 人员头像展示待办

页面路径：`/people`（人员列表）

### 18. 头像缩写统一使用姓氏首字

- 当前内容：人员列表头像使用 `LY`、`ZM`、`CA` 等英文或多字符缩写。
- 反馈：统一使用人名的第一个字（姓氏首字）。
- 后续方向：头像文本从中文姓名中提取姓氏首字，例如“林依雨”显示“林”；不再使用英文名、用户 ID 或随机缩写。
- 需要确认：
  - 无中文姓名、复姓和姓名为空时的降级规则；
  - 导入数据中的姓名字段是否始终为展示姓名；
  - 人员列表、人员详情、排行卡片和其他人员相关组件是否统一使用同一规则。

## 人员头像实施约束

- 头像缩写应由统一的姓名格式化函数生成，避免各页面自行截取或拼接。
- 该调整只影响展示，不改变用户 ID、登录名或后端人员数据。

## 用量与日志页待办

页面路径：`/usage`  
状态：待调整，暂不实施

### 19. 成本点数与成本口径区域

- 当前内容：页面展示“成本点数”指标，以及“成本口径分账”区域中的官方实际、平台估算和 CPA 估算金额。
- 反馈：成本点数意义不明；成本口径区域也不需要。
- 后续方向：移除成本点数指标和成本口径分账区域；如果未来接入真实网关或供应商账单，再根据明确的计量口径重新设计用量/账单展示。
- 需要确认：
  - 是否只保留请求数、Token、成功率和延迟等可解释指标；
  - 真实账单是否由独立网关或财务系统提供；
  - 首页、运营总览和趋势图中的成本点数是否同步移除。

### 20. 请求记录仅显示时间

- 当前内容：请求记录列表同时展示 `req-demo-001` 等请求编号和请求时间。
- 反馈：只显示时间，请求不用展示出来。
- 后续方向：列表中的“请求 / 时间”改为仅展示请求发生时间，隐藏请求编号和演示请求标识；其他人员、Key、用途、结果等必要信息按业务需要保留。
- 需要确认：
  - 请求编号是否仅从列表隐藏，还是详情、导出和检索也一并移除；
  - 是否需要保留按时间排序、时间范围筛选和精确到秒的显示；
  - 去掉请求编号后，异常请求如何定位和追踪。

### 21. 移除首 TOKEN / 总耗时列

- 当前内容：日志表格展示“首 TOKEN / 总耗时”，同时显示首个 Token 延迟和总耗时。
- 反馈：该列不用了。
- 后续方向：从日志表格中移除“首 TOKEN / 总耗时”列及其对应的筛选、排序和数据展示逻辑；如后续确有性能分析需求，再放入专门的性能详情或诊断页面。

### 22. 移除日志表格中的成本口径列

- 当前内容：日志表格展示“成本口径”列，按官方实际、平台估算、CPA 估算等口径标记金额。
- 反馈：该列也不需要。
- 后续方向：移除“成本口径”列、金额标签和对应筛选项，与页面顶部成本点数及成本口径区域的移除保持一致。

## 用量与日志页实施约束

- 成本点数和成本口径区域的移除需要同步首页、运营总览、趋势图和相关接口字段，避免同一概念在其他页面残留。
- 请求记录隐藏请求编号只影响用户界面展示，不应破坏内部日志关联、审计和故障排查能力。
- 页面当前使用的 SQLite 模拟数据标识，应与真实数据接入计划一起清理或替换。
- “首 TOKEN / 总耗时”和“成本口径”列移除后，需同步清理表头、单元格、筛选器、排序逻辑和接口字段映射。

## 告警中心待办

页面路径：`/alerts`  
状态：第一版精简，暂不实施完整规则管理

### 23. 移除实验环境告警

- 当前内容：告警汇总中包含“实验环境”数量和“未关闭实验事件”，规则卡片也带有“实验”标签。
- 反馈：不要实验环境。
- 后续方向：移除实验环境统计、实验事件、实验标签和相关筛选/状态；所有告警统一归入正式环境。
- 需要确认：
  - 后端告警数据和环境枚举是否同步删除实验分支；
  - 告警规则、事件详情、通知渠道和审计记录是否仍引用实验环境；
  - 迁移历史实验事件时，是归档、转为正式事件，还是直接清理。

### 24. 第一版暂不提供规则管理

- 当前内容：告警中心包含“事件”和“规则”两个页签，其中“规则”页签显示规则数量和规则目录。
- 反馈：第一版可以不要，先删除。
- 后续方向：第一版移除“规则”页签、规则数量、规则目录、编辑规则入口和相关管理交互；暂时只保留告警事件查看与处理。
- 需要确认：
  - 规则配置是否由后端或独立网关管理；
  - 删除前端规则页是否需要保留只读的规则来源说明；
  - 事件列表是否仍能正常展示规则触发原因。

## 告警中心实施约束

- 移除实验环境时，需要同步清理告警汇总卡片、事件标签、规则数据、筛选器和接口字段。
- 第一版删除规则管理只影响页面入口和管理能力，不应破坏后端告警触发、事件记录和处理状态。
- 后续若重新引入规则管理，应基于正式环境和真实网关事件模型重新设计，不恢复当前演示规则数据。

## 系统设置与人员管理待办

页面路径：`/settings` 及 `/people`  
状态：第一版可简化，暂不实施改动

### 25. 暂不启用登录和员工管理

- 当前情况：系统目前只有一名人员，即超级管理员。
- 反馈：当前阶段可以不登录系统，员工页面也可以不要。
- 后续方向：第一版可采用单管理员直接进入系统的方式，暂不提供登录流程；同时移除员工/人员管理页面及其导航入口，不保留无实际数据的组织与角色管理界面。
- 需要确认：
  - 是否仅在本地/内网环境跳过登录，还是所有部署环境都暂不登录；
  - 超级管理员身份是否由部署配置或服务端固定注入；
  - 后续恢复多用户时，登录、角色、组织和权限模型由独立网关还是平台自身提供。

### 26. 系统设置页第一版留空

- 当前情况：系统设置页包含组织与角色、业务口径、服务连接、缓存策略、功能开关和备份状态等多个配置区块。
- 反馈：系统设置里面整体内容可以不用这样写，先空着。
- 后续方向：第一版不展开或伪造这些配置内容，系统设置页可保留空状态或简短占位说明，等待真实配置需求和接口确定后再补充。
- 需要确认：
  - 是否保留系统设置入口，还是连同页面入口一起暂时隐藏；
  - 空状态是否只显示“暂未配置”之类的简短提示；
  - 后续配置由独立网关、部署文件还是平台管理。

## 系统设置与人员管理实施约束

- “暂不登录”是当前阶段的产品简化，不等同于永久取消认证；正式对外部署前必须重新评估认证和权限边界。
- 移除员工页面时，需要同步清理侧边栏入口、路由、人员统计、部门概览、人员筛选和依赖人员数据的演示内容。
- 如果保留系统设置页，应隐藏或移除“组织与角色”中依赖多用户的配置项，避免展示与当前单管理员状态不符的角色数量和成员数据。
- 系统设置页第一版以空状态为准，不展示没有真实来源的角色数量、服务在线数、策略配置或备份状态。
