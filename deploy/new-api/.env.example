# New API session signing secret. Keep this file local and use a long random
# value. Existing installations should keep their migrated .env value.
SESSION_SECRET=replace-with-a-long-random-new-api-session-secret
