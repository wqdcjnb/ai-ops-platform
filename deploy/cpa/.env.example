# These values are read only by the AI OPS server from the local deployment
# directory. Keep them identical to the credentials accepted by config.yaml.
CPA_MANAGEMENT_KEY=replace-with-a-long-cpa-management-key
CPA_API_KEY=replace-with-a-cpa-client-key
