# 页面功能设计与完成表

::: info 文档用途
本文是统一运营控制台的页面级功能设计、开发拆分和验收基线。内容依据更新版合并文档与[页面规划 V2](/technical/page-plan-v2)整理；“页面可打开”不等于功能完成，只有真实数据、权限、异常状态和验收证据同时具备才可标记完成。
:::

## 1. 页面数量

系统共规划 **15 个路由级页面**：

- 管理端 13 个一级页面。
- 管理端 1 个人员详情页，由人员列表进入，不占侧栏一级菜单。
- 员工端 1 个自助页面，使用独立布局。

| 编号 | 页面 | 路由 | 使用角色 | 优先级 |
| --- | --- | --- | --- | --- |
| P01 | 统一入口 | `/` | 超级管理员、管理员、部门负责人、财务 | P0 |
| P02 | 运营总览 | `/overview` | 超级管理员、管理员、部门负责人、财务 | P0 |
| P03 | 人员与部门 | `/people` | 超级管理员、管理员、部门负责人 | P1 |
| P04 | 人员详情 | `/people/:id` | 超级管理员、管理员、部门负责人 | P1 |
| P05 | Key 管理 | `/keys` | 超级管理员、管理员、部门负责人只读 | P1 |
| P06 | 额度与限流 | `/limits` | 超级管理员、管理员、部门负责人、财务只读 | P3 |
| P07 | 用途与路由 | `/routes` | 超级管理员、管理员只读 | P2 |
| P08 | 模型与渠道 | `/models` | 超级管理员、管理员、部门负责人只读 | P2 |
| P09 | 上游账号 | `/upstreams` | 超级管理员、管理员只读 | P2 |
| P10 | 用量与日志 | `/usage` | 超级管理员、管理员、部门负责人、财务 | P1 |
| P11 | 告警中心 | `/alerts` | 超级管理员、管理员、部门负责人、财务只读 | P2 |
| P12 | 审计日志 | `/audit` | 超级管理员、管理员只读 | P2 |
| P13 | 对话审计 | `/conversation-audit` | 仅超级管理员可查看正文 | P3 |
| P14 | 系统设置 | `/settings` | 超级管理员；管理员部分只读 | P3 |
| P15 | 员工自助 | `/me` | 员工本人 | P3 |

侧栏按“工作台、人员与访问、模型治理、运营监控、安全审计、系统管理”分组。员工登录后不显示管理侧栏，直接进入 `/me`。

## 2. 公共功能设计

以下能力由全部页面共享，不重复计算为独立页面。

| 功能点 | 作用 | 实现方式 | 完成条件 |
| --- | --- | --- | --- |
| 登录与会话 | 确认当前用户身份，避免匿名访问管理数据 | BFF 使用安全 Cookie 会话；生产环境启用 HTTPS、`HttpOnly`、`Secure` 和 CSRF 防护 | 未登录访问受保护路由会跳转登录；会话过期有明确提示 |
| 角色权限 | 控制全局、本部门、本人及对话正文范围 | Vue Router `meta.roles` 只控制展示；BFF 对每个接口再次执行 RBAC 和数据范围过滤 | 越权接口测试全部返回 403，不能通过直接 URL 绕过 |
| 分组侧栏 | 让高频运营、配置和审计任务清晰分区 | Vue Router 路由表生成菜单；按角色过滤；移动端使用抽屉 | 当前页、折叠状态和移动端导航正常 |
| 全局搜索 | 快速查找人员、Key 掩码和请求 ID | 顶栏搜索调用 `/api/search`，结果按类型分组；不搜索对话正文 | 结果受角色和部门范围限制，不返回完整 Key |
| 服务状态 | 分别判断 BFF、New API 和 CPA 是否可用 | BFF 主动探测上游；前端显示独立状态、检查时间和重试 | 不再使用无来源的“4/5 可用”汇总 |
| 数据来源标签 | 防止把演示或未验证数据当成真实数据 | 响应统一返回 `LIVE / PARTIAL / DEMO / UNVERIFIED` 和生成时间 | 页面首屏持续可见，导出中同样保留来源 |
| 通用筛选 | 统一时间、部门、人员、模型、渠道和状态口径 | Pinia 保存本会话筛选；查询参数同步到 URL；BFF 校验参数 | 刷新和分享 URL 后筛选可恢复 |
| 通用数据状态 | 处理加载、空数据、部分失败、无权限和首次配置 | 页面级状态组件；错误仅显示分类、请求 ID、时间和重试 | 不泄露管理密钥或上游完整错误正文 |
| 操作审计 | 追踪所有管理变更 | 写接口记录操作者、对象、变更前后摘要、来源和结果 | 写操作成功与失败都可按请求 ID 查询 |
| 响应式与可访问性 | 支持桌面高密度操作及移动查询 | Tabler 响应式布局；键盘焦点、语义标签、文字+图标状态 | 桌面、平板、手机核心流程可使用 |

## 3. 技术实现总览

```text
Vue 页面
  → Vue Router / Pinia / Zod 响应校验
  → Fastify BFF（会话、RBAC、审计、错误封装）
  → New API 适配器（人员、Key、模型、用量、日志）
  → CPA 适配器（实验账号、认证窗口、冷却与错误）
  → 平台数据库（组织扩展、用途、额度、告警、操作审计）
  → 独立审计存储（获准采集并加密的对话正文）
```

实现原则：

- 浏览器永远不直接访问 New API 或 CPA 管理接口。
- 上游返回先在 BFF 进行 Zod 校验和字段转换，页面只依赖平台自己的稳定对象。
- New API 缺少的部门、岗位、负责人、用途和多层额度字段保存在平台数据库，通过稳定 ID 映射，不直接修改转发内核。
- 调用元数据与对话正文分库存储；未开启采集的 Key 不保存正文。
- 所有列表统一使用 `{ items, page, pageSize, total }`，错误统一使用 `{ error: { code, message, requestId } }`。

## 4. 页面详细功能

### P01 统一入口

| 功能点 | 作用 | 页面表现 | 实现方式 |
| --- | --- | --- | --- |
| 四张业务入口卡 | 快速进入最常用任务 | 运营总览、人员与 Key、上游账号、待处理事项 | 聚合路由权限与摘要数据；点击进入对应页面 |
| 服务矩阵 | 判断基础组件是否在线 | BFF、New API、CPA、文档中心逐项显示状态与检查时间 | `GET /api/platform/status`；服务端并发探测并设置短时缓存 |
| 待处理事项 | 告诉管理员下一步做什么 | 认证失败、流量突增、过期 Key、未完成验证 | `GET /api/tasks/summary` 聚合告警、Key 和部署检查 |
| 首次配置清单 | 引导完成最小链路 | 渠道、模型别名、测试 Key、客户端验证 | 平台数据库保存步骤状态；已验证和人工确认分开 |
| 外部入口 | 打开现有后台和文档 | New API、CPA、项目文档 | 地址由服务端配置返回；仅保存 URL，不包含登录参数 |

### P02 运营总览

| 功能点 | 作用 | 页面表现 | 实现方式 |
| --- | --- | --- | --- |
| 五项核心指标 | 快速判断今天的运行情况 | 请求、Token、成本点数、成功率、P95 | `GET /api/overview?period=7d|30d` 聚合用量日志 |
| 趋势图 | 观察流量与成本变化 | 7/30 天请求和成本点数双序列 | BFF 按日聚合；ECharts 展示；时区固定为 Asia/Shanghai |
| 分布分析 | 找到主要消耗来源 | 人员、部门、模型别名和渠道切换 | BFF 返回统一分组数据，不在浏览器计算费用口径 |
| 目标状态表 | 找到接近或达到软目标的对象 | 正常、接近目标、已达目标、上游异常 | 80%/100% 仅标记；一期不得自动阻断请求 |
| 异常队列 | 进入具体处置流程 | 最近高优先级事件和操作入口 | 从告警接口读取；点击携带筛选条件进入告警页 |
| 渠道健康矩阵 | 判断正式与实验链路 | 成功率、延迟、请求量、认证状态 | New API 与 CPA 适配器分别提供，实验渠道统一紫色 |
| 刷新和来源 | 判断数据是否最新可信 | 更新时间、刷新、`DEMO/LIVE/PARTIAL` | 刷新取消前一请求；接口返回来源与生成时间 |

### P03 人员与部门

| 功能点 | 作用 | 页面表现 | 实现方式 |
| --- | --- | --- | --- |
| 人员检索筛选 | 快速定位员工 | 姓名、部门、岗位、状态、目标状态筛选 | `GET /api/people`，服务端分页与权限范围过滤 |
| 人员表格 | 比较人员状态和用量 | 姓名、部门、岗位、负责人、Key 数、软目标、最近调用 | New API 用户数据与平台组织扩展表合并 |
| 部门视图 | 观察部门层级和预算 | 左侧部门树或部门筛选 | 平台数据库维护组织关系；部门负责人只能看本部门 |
| 人员状态 | 处理入职、停用与离职 | 在职、停用、离职待回收 | P1 只读；P3 写接口联动停用 Key 并记录审计 |
| 批量导入 | 降低试点人员初始化成本 | CSV 模板、预检、错误行和确认 | 后置写功能；服务端事务导入，不接受包含完整 Key 的文件 |

### P04 人员详情

| 功能点 | 作用 | 页面表现 | 实现方式 |
| --- | --- | --- | --- |
| 基础档案 | 明确人员归属和状态 | 姓名、部门、岗位、负责人、在职状态 | `GET /api/people/:id`，根据角色校验数据范围 |
| 个人指标 | 判断使用和目标情况 | 点数、剩余比例、请求、Token、成功率、最近调用 | 聚合指定人员用量；口径与总览一致 |
| 个人趋势 | 观察异常变化 | 7/30 天请求、Token 和点数 | `/api/people/:id/usage`，按日返回序列 |
| Key 列表 | 管理此人的访问凭据 | 掩码、用途、模型、状态、到期和最后使用 | `/api/people/:id/keys`，绝不返回完整 Key |
| 允许模型 | 检查岗位权限 | 业务模型别名与用途 | 合并人员、用途和路由策略 |
| 管理操作 | 停用人员或调整软目标 | 确认抽屉显示影响的 Key 和生效范围 | P3 写接口；幂等、二次确认和审计 |

### P05 Key 管理

| 功能点 | 作用 | 页面表现 | 实现方式 |
| --- | --- | --- | --- |
| Key 列表与筛选 | 管理全部访问凭据 | 人员、用途、模型、状态、到期、用量、最后使用 | `GET /api/keys`，服务端分页并只返回掩码 |
| 详情抽屉 | 查看权限与来源限制 | 模型范围、有效期、设备备注、IP 限制 | `GET /api/keys/:id`；敏感字段固定脱敏 |
| 创建 Key | 为人员或系统签发独立凭据 | 人员、用途、模型、到期、备注表单 | `POST /api/keys`；明文只在成功后返回一次，不写日志 |
| 停用 Key | 处理离职、丢失和异常调用 | 二次确认及影响提示 | `POST /api/keys/:id/disable`；60 秒内验证失效并写审计 |
| 轮换 Key | 定期或紧急更换凭据 | 新 Key 一次性展示，旧 Key 按策略失效 | 幂等写接口；新旧状态与失败回滚清晰记录 |
| 连接说明 | 降低客户端配置错误 | 复制 Base URL、模型别名和非敏感步骤 | 只生成说明，绝不把管理密钥或其他人员 Key 放入页面 |

### P06 额度与限流

| 功能点 | 作用 | 页面表现 | 实现方式 |
| --- | --- | --- | --- |
| 五层额度树 | 理解公司到 Key 的预算约束 | 公司、部门、人员、用途、Key 层级 | 平台策略表维护继承关系；BFF 返回命中层级 |
| 周期与用量 | 判断剩余目标 | 小时、日、周、月上限、已用、预留、重置时间 | 聚合用量账本；统一时区和失败/重试结算规则 |
| RPM/TPM/并发 | 防止流量突增和资源抢占 | 当前限制、实时值和命中次数 | 网关执行；页面只管理经过验证的策略 |
| 软/硬模式 | 区分提示与阻断 | 一期软目标；硬模式显示上线条件 | 硬模式必须依赖额度预留、实际结算与并发一致性测试 |
| 临时调整 | 支持短期项目或审批 | 生效时间、到期时间和原因 | P3 写接口，自动到期并记录审批与审计 |

### P07 用途与路由

| 功能点 | 作用 | 页面表现 | 实现方式 |
| --- | --- | --- | --- |
| 用途目录 | 用业务语言代替供应商模型名 | 文案、客服、翻译、分析、自动化、实验 | 平台数据库维护用途、数据等级和允许岗位 |
| 模型别名 | 保持客户端配置稳定 | `ecommerce-copy` 等业务别名 | BFF 映射平台用途与 New API 模型配置 |
| 主备路由 | 明确请求实际走向 | 默认渠道、实际模型、备用模型 | `GET /api/routes`；修改前验证渠道和模型存在 |
| 正式/实验隔离 | 防止正式故障误用 Pro OAuth | 正式与实验标签、禁止跨组提示 | 服务端规则拒绝正式别名回退到 CPA 分组 |
| 降级策略 | 控制上游故障行为 | 超时、429、5xx 的重试和切换规则 | 有限重试、熔断和审计；不允许客户端指定渠道 |

### P08 模型与渠道

| 功能点 | 作用 | 页面表现 | 实现方式 |
| --- | --- | --- | --- |
| 模型目录 | 查看可用能力与价格口径 | 能力、上下文、价格、区域、状态 | `GET /api/models` 适配 New API 模型和平台扩展字段 |
| 渠道矩阵 | 比较正式和实验渠道 | 渠道、实际模型、类型、健康、延迟、成功率 | `GET /api/channels`，健康数据短时缓存 |
| 业务别名关联 | 确认模型服务哪些用途 | 别名、用途、默认/备用标记 | 与路由接口使用相同领域对象，避免重复配置 |
| 渠道详情 | 定位失败原因 | 最近错误、限流、余额状态、最近检查 | 仅返回分类和摘要，不返回上游完整响应或凭据 |
| 手动检查 | 验证配置是否恢复 | “重新检测”按钮和检查结果 | 受限服务端探测，不允许用户输入任意 URL，防止 SSRF |

### P09 上游账号

| 功能点 | 作用 | 页面表现 | 实现方式 |
| --- | --- | --- | --- |
| 官方渠道状态 | 判断正式容量是否可用 | 渠道名、健康、余额状态、最后检查 | 从 New API 渠道与健康接口适配，不展示 API Key |
| CPA 账号状态 | 管理隔离实验容量 | 在线、认证有效期、窗口、冷却、最后刷新 | CPA 管理适配器；浏览器不接触管理密钥 |
| 认证异常 | 发现即将失效或刷新失败 | 错误分类、首次发生、最后发生 | 写入告警事件；只显示可操作摘要 |
| 凭据配置状态 | 判断服务端是否已配置 | 已配置、未配置、验证失败 | 只返回布尔状态，不返回完整或可识别的密钥片段 |
| 正式/实验边界 | 防止错误使用实验账号 | 固定说明与路由隔离状态 | 服务端验证正式别名没有 CPA 隐式回退 |

### P10 用量与日志

| 功能点 | 作用 | 页面表现 | 实现方式 |
| --- | --- | --- | --- |
| 多维筛选 | 精确定位调用 | 日期、人员、部门、用途、Key、模型、渠道、状态、请求 ID | `GET /api/usage`，所有筛选由 BFF 校验并分页 |
| 汇总指标 | 理解当前筛选范围 | 请求、Token、点数、成功率、延迟 | 与列表使用同一查询条件，避免汇总和明细口径不同 |
| 调用列表 | 排查异常和核对归属 | 请求 ID、时间、人员、模型、Token、延迟、结果 | New API 日志映射为平台调用记录 |
| 详情抽屉 | 查看单次调用元数据 | 首 Token、总耗时、输入/输出 Token、渠道和错误摘要 | `GET /api/usage/:requestId`；默认没有对话正文 |
| 成本口径 | 避免估算冒充账单 | 官方实际、平台估算、CPA 估算分别标识 | BFF 返回 `costType`；不同类型不合并为实际账单 |
| 导出 | 支持核对和复盘 | CSV/Excel 异步导出、范围确认 | 服务端生成、权限过滤、敏感字段扫描并记录审计 |

### P11 告警中心

| 功能点 | 作用 | 页面表现 | 实现方式 |
| --- | --- | --- | --- |
| 告警概览 | 快速判断待处理数量 | 严重、警告、实验、已确认统计 | `GET /api/alerts/summary` |
| 事件列表 | 跟踪发生和处理过程 | 对象、规则、触发值、时间、状态、处理人 | `GET /api/alerts`，按角色过滤范围 |
| 确认与关闭 | 建立处置责任 | 填写处理说明后确认或关闭 | 幂等写接口；记录操作者和时间 |
| 告警规则 | 配置阈值和对象 | 80%/100%、突增、错误率、余额、认证到期 | 平台数据库保存；P2 先只读，P3 开放编辑 |
| 静默与通知 | 减少重复干扰 | 静默时间、通知渠道和发送结果 | 二期接企业微信/钉钉；未配置时显示灰色 |

### P12 审计日志

| 功能点 | 作用 | 页面表现 | 实现方式 |
| --- | --- | --- | --- |
| 审计筛选 | 查询谁在何时做了什么 | 操作人、动作、对象、结果、来源和日期 | `GET /api/audit-events`，不可由普通用户访问 |
| 变更摘要 | 复核配置变化 | 变更前后字段级摘要 | 写操作统一调用审计服务；敏感字段仅记录已变化 |
| 请求关联 | 连接页面操作和服务日志 | 请求 ID、目标 ID、结果码 | 全链路透传 `x-request-id` |
| 导出记录 | 满足复核需求 | 导出范围、申请人、时间和文件状态 | 导出本身再次产生审计，文件设置有效期 |
| 不可篡改要求 | 防止普通管理员清除证据 | 页面不提供删除按钮 | 正式环境使用追加写入和受限数据库权限 |

### P13 对话审计

| 功能点 | 作用 | 页面表现 | 实现方式 |
| --- | --- | --- | --- |
| 采集范围横幅 | 持续提醒当前审计边界 | 人员、Key、用途、模式、到期时间 | 读取独立审计策略；默认关闭并要求到期时间 |
| 记录筛选 | 查找获准采集的轮次 | 人员、Key、用途、模型、日期、策略、请求 ID | `/api/conversation-audits`；普通管理员只能看采集状态 |
| 查看原因门禁 | 防止无理由查看正文 | 首次打开要求填写原因 | 超级管理员权限 + 原因写入访问审计后才能解密 |
| 脱敏轮次 | 支持排障与合规复核 | 用户输入、模型回答、工具摘要、脱敏状态 | 独立存储；应用层加密；服务端解密后再做字段过滤 |
| 独立调用标识 | 避免错误拼接会话 | 无可靠会话 ID 时显示“独立调用” | 只用可靠会话 ID 归组，不按时间接近强行合并 |
| 复制/导出/删除 | 控制高风险内容操作 | 二次确认、原因和影响提示 | 每次操作生成不可由普通管理员删除的访问审计 |
| 到期清理 | 减少内容长期留存 | 到期时间和清理状态 | 后台任务删除正文，保留不含内容的删除证明 |

### P14 系统设置

| 功能点 | 作用 | 页面表现 | 实现方式 |
| --- | --- | --- | --- |
| 组织与角色 | 配置管理范围 | 角色、部门负责人、财务只读范围 | 平台用户与角色表；高权限变更二次确认 |
| 业务口径 | 固定全站计算规则 | 时区、预算单位、周期、失败/重试计费 | 版本化设置；变更前展示影响并写审计 |
| 服务连接 | 查看上游配置是否完整 | 地址、已配置、验证状态、最后检查 | 地址来自服务端；凭据通过部署环境配置，不回传浏览器 |
| 数据留存 | 控制日志和对话保存时间 | 元数据、对话、导出文件留存天数 | 后台清理任务和执行记录；正文留存采用最短必要期限 |
| 功能开关 | 控制未验收能力 | 对话采集、写操作、硬额度、导出 | 服务端开关优先；前端仅反映状态 |
| 备份状态 | 判断能否恢复 | 最近备份、校验、恢复演练时间 | 只展示作业元数据，不提供浏览器直接下载数据库 |

### P15 员工自助

| 功能点 | 作用 | 页面表现 | 实现方式 |
| --- | --- | --- | --- |
| 我的 Key | 查看个人凭据状态 | 掩码、用途、模型、有效期、最后使用 | `GET /api/me/keys`，只能读取当前会话用户 |
| 连接说明 | 完成客户端配置 | Codex Desktop / WorkBuddy 步骤与复制项 | 只展示个人已授权配置，不显示上游或管理凭据 |
| 我的用量 | 了解软目标和趋势 | 点数、剩余比例、请求、Token、7/30 天趋势 | `GET /api/me/usage`，口径与管理端一致 |
| 可用模型 | 选择合适业务能力 | 业务别名、用途和使用建议 | `GET /api/me/models`，隐藏供应商和实际渠道 |
| 常见错误 | 帮助自行排障 | 鉴权、模型无权限、软目标、上游异常说明 | 根据稳定错误码提供建议，不显示内部配置 |
| 临时额度申请 | 支持短期高价值任务 | 申请原因、额度和有效期 | 二期功能；负责人审批并自动到期 |

## 5. BFF 接口清单

| 资源 | 只读接口 | 后续写接口 |
| --- | --- | --- |
| 平台 | `GET /api/platform/status`、`GET /api/tasks/summary`、`GET /api/search`、`GET /api/integrations/new-api/management` | — |
| 总览 | `GET /api/overview` | — |
| 人员 | `GET /api/people`、`GET /api/people/:id`、`GET /api/people/:id/usage` | `POST /api/people/import`、`PATCH /api/people/:id` |
| Key | `GET /api/keys`、`GET /api/keys/:id` | `POST /api/keys`、`POST /api/keys/:id/disable`、`POST /api/keys/:id/rotate` |
| 额度 | `GET /api/limits` | `PATCH /api/limits/:id` |
| 路由 | `GET /api/routes` | `PATCH /api/routes/:id` |
| 模型渠道 | `GET /api/models`、`GET /api/channels` | `POST /api/channels/:id/check` |
| 上游 | `GET /api/upstreams` | 认证配置仅通过受保护部署流程处理 |
| 用量 | `GET /api/usage`、`GET /api/usage/:requestId` | `POST /api/usage/exports` |
| 告警 | `GET /api/alerts`、`GET /api/alerts/summary`、`GET /api/alerts/:id`、`GET /api/alert-rules` | `POST /api/alerts/:id/acknowledge`、`POST /api/alerts/:id/close` |
| 审计 | `GET /api/audit-events`、`GET /api/audit-events/:id` | 导出接口 |
| 对话审计 | `GET /api/conversation-audits`、`POST /api/conversation-audits/:id/access` 演示原因门禁 | 真实解密、复制、导出、删除和策略接口 |
| 设置 | `GET /api/settings` | `PATCH /api/settings/:section` |
| 员工自助 | `GET /api/me`、`GET /api/me/keys`、`GET /api/me/usage`、`GET /api/me/models` | 二期额度申请 |

## 6. 页面完成定义

单个页面必须同时满足以下条件才标记为“完成”：

- [ ] 路由、侧栏、标题和权限范围已经实现。
- [ ] BFF 接口契约、Zod 校验和稳定错误结构已经实现。
- [ ] 已接真实数据，或在页面持续明确标记 `DEMO / UNVERIFIED`。
- [ ] 加载、空数据、部分失败、无权限和首次配置状态齐全。
- [ ] 桌面和移动布局通过视觉检查。
- [ ] 金额、Token、百分比和延迟口径通过单元测试。
- [ ] 页面组件测试和关键流程端到端测试通过。
- [ ] 浏览器资源、网络响应、日志和导出中不存在管理密钥。
- [ ] 写操作具备服务端授权、幂等、二次确认、审计和回滚说明。
- [ ] 验收证据已记录到完成表。

## 7. 当前完成表

状态说明：`已完成`表示满足当前任务的验收条件；`部分完成`表示只有页面骨架或演示契约；`未开始`表示尚无对应路由；`受阻`表示依赖外部决策或凭据。

### 7.1 公共基础

| 编号 | 工作项 | 当前状态 | 已完成内容 | 剩余工作 | 验收证据 |
| --- | --- | --- | --- | --- | --- |
| F01 | Vue/Fastify 工作区 | 已完成 | Vue、Vite、Fastify、Zod、Vitest 基线 | 持续维护 | 类型检查、测试和生产构建通过 |
| F02 | BFF 安全基础 | 部分完成 | Helmet、CORS、限流、请求 ID、禁用缓存；SQLite 账号种子与密码摘要校验、HttpOnly 会话 Cookie、会话查询/退出、服务端 RBAC 路径策略和 401/403 错误边界 | 数据库组织映射、CSRF/MFA、会话持久化、完整审计上下文和生产身份源接入 | Server 认证与数据库测试、登录/员工越权接口抽查、浏览器管理员与员工登录验证 |
| F03 | New API 连通状态 | 已完成 | 公开状态探测、认证状态区分、不返回凭据 | 增加版本兼容记录 | `/api/integrations/new-api/status` |
| F04 | New API 管理适配器 | 部分完成 | 服务端管理客户端、Bearer 与 `New-Api-User` 兼容头、模型/渠道只读端点、认证/不可达/地址无效安全降级、管理员保护的能力状态接口；本地配置自动加载与真实管理认证、模型元数据、渠道列表和渠道模型能力只读验证 | 模型/渠道字段映射与真实空状态、用户列表/Key/日志接口验证、短时缓存、上游错误分类和正式环境兼容验收 | 适配器 4 项测试、管理状态接口权限与敏感字段检查、类型检查通过；本机 BFF 返回 ready，模型与渠道能力均 available |
| F05 | CPA 状态适配器 | 部分完成 | 本机 CPA 入口可达性探测、独立状态展示 | 管理认证、账号状态与认证窗口字段 | `/api/platform/status` 实时探测 |
| F06 | Vue Router 与权限布局 | 已完成 | Vue Router、Pinia、管理端分组布局、员工端独立布局、角色路由守卫、登录页、会话初始化、退出登录和页面标题 | 登录状态实时刷新与身份源切换 | 13 项前端路由/认证测试、生产构建和浏览器路由检查通过 |
| F07 | 平台数据库 | 部分完成 | Node 24 内置 SQLite 适配器、版本迁移表、部门/用户/API Key/额度策略/审计事件核心表、WAL/外键约束、数据库状态接口、演示账号种子和密码摘要登录，可重复的组织/Key/额度/审计模拟数据种子，Key 模型列表字段迁移，以及额度和审计页面读取 | 会话持久化、真实组织/Key/用量映射、完整页面数据访问仓储、备份与恢复演练 | 数据库迁移/模拟数据种子/账号登录/Key 创建/额度读取/审计读取测试、BFF 数据库状态接口和类型检查通过 |
| F08 | 独立对话审计存储 | 未开始 | — | 采集、脱敏、加密、访问审计、到期清理 | — |

### 7.2 页面进度

| 页面 | 状态 | 已完成 | 未完成 | 下一验收点 |
| --- | --- | --- | --- | --- |
| P01 统一入口 | 部分完成 | 独立 `/` 路由、四张业务入口卡、BFF/New API/CPA/文档实时服务矩阵、配置待办、首次配置清单、安全外部入口、部分失败状态、桌面与手机布局 | 人员/上游/告警目标页完成后开放全部卡片跳转；待办接入真实告警和 Key 数据；正式会话与 RBAC | 18 项前后端测试、生产构建、接口无敏感字段检查及桌面/390px 浏览器检查通过 |
| P02 运营总览 | 部分完成 | 独立 `/overview` 路由、页面视觉、五项指标、7/30 天趋势、演示人员/告警/渠道、BFF/New API 实际状态 | 真实用量、四类分布、CPA 状态、服务端权限 | New API 真实数据替换演示指标 |
| P03 人员与部门 | 部分完成 | 独立 `/people` 路由、角色守卫、SQLite 人员/部门列表、添加本地演示人员、人员摘要、部门概览、检索与组合筛选、分页表格、软目标、人员/Key/状态字段、人员详情跳转、加载/失败/空结果、桌面与手机布局 | New API 真实人员映射、完整职位/用途/用量仓储、服务端数据范围、批量导入和生产写操作审计 | 人员列表/添加/重复用户名接口测试、筛选接口抽查、接口无敏感字段检查、类型检查和生产构建 |
| P04 人员详情 | 部分完成 | 独立 `/people/:id` 路由、角色守卫、基础档案、个人指标、7/30 天趋势、掩码 Key、允许模型、软目标、未找到错误、Key 管理跳转、只读操作边界及响应式布局 | New API 真实详情/用量/Key 映射、服务端会话和数据范围及人员管理写操作 | 27 项前后端测试、30 天接口抽查、完整 Key 模式检查、桌面与 390px 浏览器检查通过 |
| P05 Key 管理 | 部分完成 | 独立 `/keys` 路由、角色守卫、SQLite Key 摘要读取与创建、人员/用途/模型/状态筛选、分页、掩码列表、一次性完整 Key 展示、临期状态、详情抽屉、来源与限流、连接说明、未找到错误及响应式布局 | New API 真实 Key 映射、服务端数据范围、IP/设备实际策略，以及停用和轮换写操作 | Key 列表/创建/权限拒绝测试、临期筛选与详情接口抽查、完整 Key 不回显检查、类型检查和生产构建 |
| P06 额度与限流 | 部分完成 | 独立 `/limits` 路由、角色守卫、可逐级或全部折叠的五层额度树、层级与搜索筛选、SQLite 月度软目标读取、小时/日/周演示计量、已用/预留/重置时间、RPM/TPM/并发观测、命中次数、硬额度上线条件、禁用写操作及响应式布局 | 实时用量账本、New API 真实字段映射、服务端会话和数据范围、并发预留/结算验证，以及策略调整、硬额度和临时申请写操作 | 额度来源与月度覆盖测试、筛选接口与硬额度关闭状态抽查、折叠交互、生产构建、桌面与 390px 浏览器检查通过 |
| P07 用途与路由 | 部分完成 | 独立 `/routes` 路由、角色守卫、用途摘要、用途/环境/状态与关键词筛选、业务别名、主备渠道、实际模型、健康与用量指标、详情抽屉、超时/429/5xx/熔断规则、数据与岗位边界、服务端固定禁止客户端选渠道和正式/实验跨组回退、禁用写操作及响应式布局 | 平台用途/路由表、New API 真实模型与渠道映射、服务端会话、配置校验、操作审计、回滚及写接口 | 42 项前后端测试、实验筛选与隔离字段抽查、生产构建、桌面与 390px 浏览器检查通过 |
| P08 模型与渠道 | 部分完成 | 独立 `/models` 路由、角色守卫、模型摘要与组合筛选、能力/上下文/区域/状态目录、官方账单价与 CPA 估算价口径、业务别名与主备用途、正式/实验渠道健康矩阵、延迟/成功率/余额/限流、仅含分类摘要和凭据配置布尔值的安全详情抽屉、禁用手动检测与写操作及响应式布局 | New API 真实模型/渠道适配、健康数据短时缓存、受限服务端手动探测、服务端会话与 RBAC、价格对账及配置写操作 | 47 项前后端测试、实验价格口径与降级渠道接口抽查、凭据不泄露检查、生产构建、桌面与 390px 浏览器检查通过 |
| P09 上游账号 | 部分完成 | 独立 `/upstreams` 路由、管理员角色守卫、New API/CPA 实时连通探测、账号摘要与组合筛选、官方余额/容量/健康状态、CPA 认证有效期与最后刷新、5 小时和周窗口、冷却状态、认证异常首末发生时间、安全详情抽屉、凭据配置/验证状态、固定正式/实验隔离及响应式布局 | New API 真实渠道与余额适配、CPA 管理认证及真实账号窗口适配、服务端会话与 RBAC、告警事件落库、受保护的重新授权/验证流程和操作审计 | 52 项前后端测试、CPA 认证异常筛选与实时探测接口抽查、凭据不泄露检查、生产构建、桌面与 390px 浏览器检查通过 |
| P10 用量与日志 | 部分完成 | 独立 `/usage` 路由、角色守卫、日期/人员/部门/用途/Key/模型/渠道/结果/成本口径/请求 ID 筛选、同条件汇总、分页调用列表、输入输出 Token/点数/首 Token/总耗时、官方实际/平台估算/CPA 估算独立分账、请求详情抽屉、错误摘要、请求 ID 透传状态、固定无对话正文及响应式布局 | New API 真实日志与计费字段映射、服务端会话和本人/部门/全局数据范围、官方账单对账、CPA 消耗映射、真实请求 ID 链路验证、异步导出及导出审计 | 57 项前后端测试、CPA 成本筛选与详情接口抽查、正文和凭据不泄露检查、生产构建、桌面与 390px 浏览器检查通过 |
| P11 告警中心 | 部分完成 | 独立 `/alerts` 路由、角色守卫、待处理/严重/警告/实验/已确认汇总、严重度/状态/来源/环境/关键词筛选、分页事件列表、告警对象/规则/触发值/阈值/次数/处理人与静默状态、事件安全详情抽屉、处理时间线、关联请求 ID、6 类规则目录、通知未配置状态、禁用确认/关闭/静默/规则编辑及响应式布局 | New API 与平台数据库真实事件采集、规则持久化、服务端会话和全局/部门数据范围、企业微信/钉钉通知、确认/关闭/静默写接口及操作审计 | 62 项前后端测试、严重事件与实验筛选接口抽查、正文和凭据不泄露检查、生产构建、事件/规则/详情及 390px 浏览器检查通过 |
| P12 审计日志 | 部分完成 | 独立 `/audit` 路由、超级管理员/管理员角色守卫、SQLite 审计事件读取、事件/成功/失败/拒绝/敏感变化汇总、日期/操作人/动作/对象/结果/来源/请求 ID 组合筛选、分页事件表格、字段级前后变化摘要、脱敏来源信息、结果码、请求关联、安全详情抽屉、页面不可删除与不可篡改待验证状态、禁用导出及响应式布局 | 所有写操作统一审计、真实 x-request-id 全链路、追加写入、哈希链或等效完整性保护、受限数据库权限、审计导出与导出再审计 | 46 项服务端、48 项前端测试、SQLite 读取与敏感字段边界抽查、生产构建、列表/详情及 390px 浏览器检查通过 |
| P13 对话审计 | 部分完成 | 独立 `/conversation-audit` 路由、超级管理员专属前端与服务端 RBAC、审计/获准采集/仅元数据/临期/脱敏待复核汇总、人员/Key/用途/模型/日期/策略/采集状态/脱敏状态/归组/请求 ID 组合筛选、记录分页、采集范围与到期提示、可靠会话和独立调用标识、查看原因与确认门禁、预先脱敏的合成用户/助手/工具轮次、关联请求 ID、固定原始内容不可用、复制/导出/删除禁用及响应式布局 | 独立采集服务与存储、SSE 组装、真实字段脱敏、应用层加密及外置密钥、查看原因访问审计持久化、可靠会话归组、到期清理与无正文删除证明，以及复制/导出/删除二次确认和审计 | 72 项前后端测试、原因长度门禁与独立调用筛选接口抽查、合成/未解密/未持久化边界检查、生产构建、1058px 与 390px 浏览器检查通过 |
| P14 系统设置 | 部分完成 | 独立 `/settings` 路由、超级管理员/管理员角色守卫、设置分区/角色/服务在线/高风险功能汇总、组织与五类角色数据范围、版本化业务口径展示、BFF/New API/CPA/文档实时连接探测、安全地址与凭据配置布尔值、调用元数据/对话正文/操作审计/导出文件留存、管理写操作/对话采集/硬额度/数据导出/员工入口五项服务端开关关闭状态、备份未配置状态、六分区切换、禁用编辑/验证/恢复与下载操作及响应式布局 | 平台组织与角色表、业务口径版本持久化与对账、服务端受限验证、后台清理任务与执行记录、功能开关持久化、设置变更二次确认/审计/回滚，以及数据库备份、校验和恢复演练 | 77 项前后端测试、混合数据来源与凭据不泄露接口抽查、生产构建、组织/连接/功能开关分区及 1058px/390px 浏览器检查通过 |
| P15 员工自助 | 部分完成 | 独立 `/me` 路由与员工布局、登录会话与员工服务端 RBAC、仅本人范围提示、2 个掩码 Key、7/30 天本人用量与用途占比、Codex Desktop/WorkBuddy 连接说明、3 个业务模型别名、4 类稳定错误码帮助、禁用轮换与临时额度申请，以及服务端固定不提供其他人员、完整 Key、供应商、实际模型和上游渠道 | New API 人员/Key/用量/模型映射、完整 Key 首次受控交付、Key 轮换，以及临时额度申请审批与自动到期 | 87 项前后端测试、四组本人接口安全字段抽查、类型检查、生产构建、7/30 天与 WorkBuddy/错误展开交互，以及 1058px/390px 浏览器检查通过 |

当前统计：**15 个页面中 0 个全部完成、15 个部分完成、0 个未开始**。公共基础中 3 项完成、3 项部分完成或受阻、2 项未开始。该统计按正式完整验收口径计算；已完成的本机只读/演示页面仍需真实上游数据和服务端权限后才升级为“已完成”。

## 8. 建议开发批次

| 批次 | 页面/基础 | 主要产出 | 退出条件 |
| --- | --- | --- | --- |
| 第 1 批 P0 | F06、P01、P02 | Vue Router、分组侧栏、统一入口、总览独立路由 | 页面状态真实，演示与实时数据清楚区分 |
| 第 2 批 P1A | F04、P03、P04 | New API 人员适配、人员列表与详情 | 分页、权限范围和字段映射通过测试 |
| 第 3 批 P1B | P05、P10 | 掩码 Key 列表、用量与日志 | 不泄露凭据，抽样用量可与 New API 核对 |
| 第 4 批 P2 | F05、P07、P08、P09、P11、P12 | 模型治理、上游状态、告警与操作审计 | 正式/实验隔离和异常状态通过测试 |
| 第 5 批 P3 | F07、P06、P14、P15 | 平台配置、软目标、员工自助 | RBAC 和本人/部门/全局数据范围通过 |
| 第 6 批敏感功能 | F08、P13 | 对话采集、脱敏正文与访问审计 | 加密、到期清理、超级管理员门禁全部通过 |
| 第 7 批写操作 | Key、额度、路由、告警写接口 | 幂等、二次确认、审计和回滚 | 并发、安全与恢复测试通过 |

当前停点：F02 登录、会话与服务端 RBAC 的最小闭环已完成，F04 已完成 New API 管理适配器的服务端安全基础、本地配置自动加载和真实管理连接验证，F07 已建立 SQLite 核心表、迁移机制、账号种子登录和一组可重复的模拟组织/Key/额度/审计数据；P03 已接入 SQLite 人员列表与管理员添加人员，P05 已接入 SQLite Key 列表与管理员一次性创建 Key，P06 已接入 SQLite 月度软额度目标读取，P12 已接入 SQLite 审计事件列表与详情读取。15 个规划页面均已有可运行原型；当前 New API 模型元数据和渠道配置列表为空，下一步先完成模型/渠道字段映射与真实空状态，再替换人员、Key、用量页面的演示数据，同时保持硬额度和高风险写操作关闭。P15 正式化仍依赖 New API 人员/Key/用量/模型映射、个人 Key 首次受控交付与轮换，以及临时额度申请审批和自动到期。
